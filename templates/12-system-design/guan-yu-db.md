# 关于db

relational：

* 好久没听的一个词，impedance mismatch，就是db relationship与data structure不相容。
* structured data用它





non-relational：

* semi-structured data或者unstructured的用它
* 主要有这几款：document store（MongoDB）, columnar database（Cassandra）, key-value store（DynamoDB）, and graph database（Neo4jDB）
*
