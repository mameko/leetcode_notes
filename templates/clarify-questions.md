# 8 clarify questions

## Tree

Binary Tree ? Binary Search Tree ? full tree (heap) ? has parent pointer or not ?

## Number

Int还是float ？ positive 或 negative, 允不允许前导0

## Arrays

有序无序？有无duplicate？元素类型，升序还是降序，能不能全放memory里； 如果给2条arrays，两条有没有dup，等不等长，是不是matrix, 有没有序, range譬如，int能不能放下。etc.

## General

存储方式，输入输出，方法接口，是否重复访问（stream不能），处理之后需不需要保持原来顺序。

## String

大小写>\_<
